# Build with
docker build -t sparkplug-client .

# Run with
docker run --rm sparkplug-client

# Run with Debug
docker run --rm -it sparkplug-client

#  -it means interactive + TTY, so stdout/stderr is shown immediately.

